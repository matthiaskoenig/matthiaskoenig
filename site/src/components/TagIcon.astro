---
import { iconPaths } from '../lib/icons.ts';
interface Props { icon: keyof typeof iconPaths; class?: string }
const { icon, class: cls = 'h-5 w-5' } = Astro.props;
---
<svg viewBox="0 0 24 24" class={`${cls} fill-current`} aria-hidden="true"><path d={iconPaths[icon]} /></svg>
